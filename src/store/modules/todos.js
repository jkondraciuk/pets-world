const state = {
    text: 'TEST'
}

const getters = {
    getText: (state) => state.text
}

const actions = {}

const mutations = {}

export default {
    state,
    getters,
    actions,
    mutations
}